name: Terraform Workspaces

on:
  push:
    branches:
      - Master
      - staging
      - development
  pull_request:
    branches:
      - Master
      - staging
      - development

jobs:
  terraform:
    runs-on: ubuntu-latest

    env:
      TF_WORKSPACE: ${{ github.ref == 'refs/heads/Master' && 'prod' || github.ref == 'refs/heads/staging' && 'staging' || 'development' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: 1.5.0

      - name: Initialize Terraform
        run: terraform init

      - name: Unset TF_WORKSPACE and Select or Create Workspace
        run: |
          unset TF_WORKSPACE
          terraform workspace select ${{ env.TF_WORKSPACE }} || terraform workspace new ${{ env.TF_WORKSPACE }}

      - name: Terraform Plan
        run: terraform plan -out=tfplan

      - name: Terraform Apply
        if: github.ref == 'refs/heads/Master'
        run: terraform apply tfplan
